import { createFileRoute } from "@tanstack/react-router";
import { QuoteForm } from "@/components/QuoteForm";
import logo from "@/assets/logo.png";
import wordmark from "@/assets/putslaget-wordmark.png";

export const Route = createFileRoute("/")({
  head: () => ({
    meta: [
      { title: "Putslaget – Fönsterputs som glänser" },
      { name: "description", content: "Putslaget är ett lokalt fönsterputsföretag drivet av Leon och Jordie. Begär en kostnadsfri offert på minuten." },
    ],
  }),
  component: Index,
});

function Nav() {
  return (
    <header className="sticky top-0 z-40 backdrop-blur-md" style={{ background: "var(--color-glass)" }}>
      <div className="mx-auto flex max-w-6xl items-center justify-between px-6 py-4">
        <a href="#top" className="flex items-center gap-2">
          <img src={logo} alt="Putslaget logotyp" className="h-16 w-16 rounded-full object-contain" />
          <img src={wordmark} alt="Putslaget" className="h-28 w-auto object-fill" />
        </a>
        <nav className="hidden gap-8 text-sm text-muted-foreground sm:flex">
          <a href="#tjanster" className="transition hover:text-foreground">Tjänster</a>
          <a href="#om" className="transition hover:text-foreground">Om oss</a>
          <a href="#offert" className="transition hover:text-foreground">Offert</a>
        </nav>
        <a
          href="#offert"
          className="rounded-full bg-foreground px-4 py-2 text-xs font-medium text-background transition hover:opacity-90"
        >
          Begär offert
        </a>
      </div>
    </header>
  );
}

function Hero() {
  return (
    <section id="top" className="relative mx-auto max-w-6xl px-6 pt-20 pb-24 sm:pt-28 sm:pb-32">
      <p className="mb-6 inline-flex items-center gap-2 rounded-full border bg-card px-3 py-1 text-xs text-muted-foreground">
        <span className="h-1.5 w-1.5 rounded-full bg-primary" />
        Lokalt drivet i Värmdö
      </p>
      <h1 className="max-w-3xl text-5xl leading-[1.05] sm:text-7xl">
        Fönster som <em className="not-italic text-primary">glänser</em>.<br />
        Inget krångel.
      </h1>
      <p className="mt-6 max-w-xl text-muted-foreground whitespace-pre-line text-xs">
        Vi är två drivna 18-åringar som valt att bygga Putslaget{"\n"}
        istället för att sitta still. För oss handlar företagandet om att{"\n"}
        ta ansvar och visa handlingskraft. Vi satsar all vår energi på{"\n"}
        att leverera tjänster med hög kvalitet.
      </p>
      <div className="mt-10 flex flex-wrap gap-3">
        <a
          href="#offert"
          className="rounded-md bg-primary px-6 py-3 text-sm font-medium text-primary-foreground shadow-sm transition hover:bg-primary/90"
        >
          Begär offert
        </a>
        <a
          href="#tjanster"
          className="rounded-md border bg-background px-6 py-3 text-sm font-medium transition hover:bg-secondary"
        >
          Se tjänster
        </a>
      </div>
    </section>
  );
}

function Services() {
  const items = [
    { t: "Villor & radhus", d: "Inifrån, utifrån eller båda. Inklusive karmar och fönsterbleck." },
    { t: "Lägenheter", d: "Snabbt, prydligt och alltid med skor av — eller skoskydd." },
    { t: "Kontor & lokaler", d: "Återkommande puts efter ert schema. Fakturering eller Swish." },
  ];
  return (
    <section id="tjanster" className="border-t bg-card/50">
      <div className="mx-auto max-w-6xl px-6 py-20 sm:py-28">
        <h2 className="max-w-2xl text-4xl sm:text-5xl">Tjänster</h2>
        <p className="mt-4 max-w-xl text-muted-foreground">Tre enkla paket. Allt annat löser vi tillsammans.</p>
        <div className="mt-12 grid gap-4 sm:grid-cols-3">
          {items.map((it, i) => (
            <div key={it.t} className="rounded-xl border bg-background p-6 transition hover:-translate-y-0.5 hover:shadow-sm">
              <span className="text-xs text-muted-foreground">0{i + 1}</span>
              <h3 className="mt-3 text-xl">{it.t}</h3>
              <p className="mt-2 text-sm text-muted-foreground">{it.d}</p>
            </div>
          ))}
        </div>
      </div>
    </section>
  );
}

function About() {
  return (
    <section id="om" className="mx-auto max-w-6xl px-6 py-20 sm:py-28">
      <div className="grid gap-12 sm:grid-cols-2 sm:gap-20">
        <div>
          <h2 className="text-4xl sm:text-5xl">Leon & Jordie</h2>
          <p className="mt-6 text-muted-foreground">
            Vi är 17 och 19 år gamla och driver Putslaget tillsammans. Det började
            som ett sommarjobb i kvarteret — idag är vi kvar för att vi älskar
            jobbet och kunderna kommer tillbaka.
          </p>
          <p className="mt-4 text-muted-foreground">
            Ärlig prissättning, punktlighet och fönster som faktiskt blir rena.
          </p>
        </div>
        <div className="rounded-xl border bg-card p-8">
          <dl className="grid grid-cols-2 gap-6">
            <div>
              <dt className="text-xs uppercase tracking-wider text-muted-foreground">Grundat</dt>
              <dd className="mt-1 text-3xl font-display">2024</dd>
            </div>
            <div>
              <dt className="text-xs uppercase tracking-wider text-muted-foreground">Nöjda kunder</dt>
              <dd className="mt-1 text-3xl font-display">120+</dd>
            </div>
            <div>
              <dt className="text-xs uppercase tracking-wider text-muted-foreground">Svarstid</dt>
              <dd className="mt-1 text-3xl font-display">&lt;24h</dd>
            </div>
            <div>
              <dt className="text-xs uppercase tracking-wider text-muted-foreground">Garanti</dt>
              <dd className="mt-1 text-3xl font-display">100%</dd>
            </div>
          </dl>
        </div>
      </div>
    </section>
  );
}

function QuoteSection() {
  return (
    <section id="offert" className="border-t bg-card/50">
      <div className="mx-auto max-w-3xl px-6 py-20 sm:py-28">
        <div className="mb-10 text-center">
          <h2 className="text-4xl sm:text-5xl">Begär offert</h2>
          <p className="mx-auto mt-4 max-w-md text-muted-foreground">
            Fyll i några snabba detaljer så återkommer vi med ett pris.
          </p>
        </div>
        <QuoteForm />
      </div>
    </section>
  );
}

function Footer() {
  return (
    <footer className="border-t">
      <div className="mx-auto flex max-w-6xl flex-col items-start justify-between gap-4 px-6 py-10 sm:flex-row sm:items-center">
        <div className="flex items-center gap-2">
          <img src={logo} alt="Putslaget logotyp" className="h-7 w-7 rounded-full object-contain" />
          <img src={wordmark} alt="Putslaget" className="h-5 w-auto object-contain" />
        </div>
        <p className="text-xs text-muted-foreground">© {new Date().getFullYear()} Putslaget — Leon & Jordie.</p>
      </div>
    </footer>
  );
}

function Index() {
  return (
    <main>
      <link rel="preconnect" href="https://fonts.googleapis.com" />
      <link rel="preconnect" href="https://fonts.gstatic.com" crossOrigin="" />
      <link href="https://fonts.googleapis.com/css2?family=Fraunces:opsz,wght@9..144,400;9..144,500;9..144,600&family=Inter:wght@400;500;600&display=swap" rel="stylesheet" />
      <Nav />
      <Hero />
      <Services />
      <About />
      <QuoteSection />
      <Footer />
    </main>
  );
}
